
const { calMul } = require('./mul');
const {z, x, squareNum} = require('./ex1')


module.exports = {calMul, z, x, squareNum}


//best practice is to keep all exports in one file together, it will be to access
